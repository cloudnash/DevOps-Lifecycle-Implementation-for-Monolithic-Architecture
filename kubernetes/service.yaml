apiVersion: v1
kind: Service
metadata:
  name: analytics-app-service
  namespace: analytics-prod
spec:
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 8080
    nodePort: 30008
  selector:
    app: analytics-app
